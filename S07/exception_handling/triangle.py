def is_triangle(a, b, c) -> bool:
    # invalid type --> raise TypeError


def type_of_triangle(a, b, c):
    if not is_triangle(a, b, c):
        # if it's not a triangle --> Value Error

    else:


is_triangle("hello", 10, 20)